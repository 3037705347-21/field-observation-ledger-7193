# 修复前故障复现（Docker）

## 项目与标准命令
项目使用 Go 分析模块计算观察窗口。镜像构建后，在容器内执行 `go build ./...`，再执行观察窗口诊断测试。

## 环境构建与编译
当前平台的镜像构建和容器内 `go build ./...` 已通过。

## 故障触发步骤
准备两条记录，第一条时间为 `2026-08-01T23:30:00Z`，第二条时间为 `2026-08-02T00:30:00Z`，计算观察窗口天数。

## 实际错误输出
`expected two calendar days, got 1`

## 期望行为
跨越两个自然日的观察窗口应计算为两天，即使两个时间点间隔不足 24 小时。

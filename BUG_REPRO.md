# 修复前故障复现（Docker）

## 项目与标准命令
项目使用 Go HTTP 服务和物种目录。镜像构建后，在容器内执行 `go build ./...`，再检查查询结果的标签隔离。

## 环境构建与编译
当前平台的镜像构建和容器内 `go build ./...` 已通过。

## 故障触发步骤
创建一个带有多个标签的物种，修改一次查询结果中的第一个标签，再次查询同一物种；随后修改列表结果中的第二个标签并再次查询。

## 实际错误输出
`repository state changed through returned tags: []string{"mutated", "night"}`

## 期望行为
修改查询或列表返回值不应改变仓储状态，后续查询应继续返回原始标签。

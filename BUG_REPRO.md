# 修复前故障复现（Docker）

## 项目与标准命令
项目使用 Go 服务生成观察摘要。镜像构建后，在容器内执行 `go build ./...`，再执行摘要诊断测试。

## 环境构建与编译
当前平台的镜像构建和容器内 `go build ./...` 已通过。

## 故障触发步骤
创建并审核一条观察记录，再构建包含全部记录的摘要，比较摘要中的审核事件数量和实际审核历史数量。

## 实际错误输出
`expected one review event from review history, got 3`

## 期望行为
摘要中的审核事件数量应等于实际审核历史中的事件数量。
